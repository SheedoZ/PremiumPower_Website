# تحليل مشروع Premium Power Website

تاريخ التحليل: 2026-09-01
الفرع: `claude/analysis-e13t67`
الملف المفحوص: `index.html` (2482 سطرًا، 190 كيلوبايت)

طريقة الفحص: قراءة الكود كاملًا + تشغيل الموقع محليًا على متصفح Chromium بلا واجهة، وتنفيذ اختبارات فعلية على النماذج والحاسبة والترجمة والتجاوب والوصولية. كل مشكلة أدناه مُثبتة بنتيجة اختبار، وليست استنتاجًا نظريًا.

---

## الخلاصة أولًا

الموقع مبني بعناية من ناحية المحتوى والتصميم، والترجمة العربية كاملة 100% (صفر مفاتيح ناقصة). لكن توجد **ثلاث مشاكل حرجة** تُفقد الموقع وظيفته الأساسية:

1. نموذج التواصل — وهو المسار الوحيد لجمع العملاء — **يرفض صيغة رقم الهاتف المصرية المحلية**، وهي نفس الصيغة التي يطلبها الموقع من المستخدم.
2. **جميع عناوين الأقسام الثمانية عشر** تفقد كسر السطر ولون التمييز فور تحميل الصفحة، في اللغتين.
3. **البريد الإلكتروني معطوب** ويظهر حرفيًا كنص `[email protected]` مع رابط يؤدي إلى صفحة 404.

بالإضافة إلى ~13 ميجابايت من الأصول غير المُحسَّنة، وصور مخزون معروضة على أنها مشاريع وعملاء حقيقيون.

---

## أولًا: مشاكل حرجة

### 1. التحقق من رقم الهاتف يرفض الصيغة المصرية المحلية

الموقع يطلب من المستخدم صيغة معينة في حقل الإدخال:

```html
<!-- index.html:1784 -->
<input id="f-phone" type="tel" class="form-input" placeholder="01XXXXXXXXX" required aria-required="true">
```

ثم يرفض التحقق نفس هذه الصيغة بالضبط:

```javascript
// index.html:2388
if(!/^(\+?20)?1[0-9]{9}$/.test(phone.replace(/\s/g,''))){
```

سبب العطل: النمط يتوقع بادئة اختيارية ثم الرقم 1 مباشرة. الصفر الأول في الرقم المحلي لا يطابق أي جزء من النمط، فيفشل التحقق.

**نتيجة الاختبار الفعلي:**

| المُدخَل | النتيجة |
|---|---|
| `01229688688` | مرفوض ❌ |
| `0122 968 8688` | مرفوض ❌ |
| `0122-968-8688` | مرفوض ❌ |
| `00201229688688` | مرفوض ❌ |
| `0227356789` | مرفوض ❌ |
| `+201229688688` | مقبول ✅ |

الصيغة الوحيدة المقبولة عمليًا هي الصيغة الدولية، وهي ليست ما يكتبه المستخدم المصري عادةً، وليست ما يطلبه الموقع منه.

**الأثر:** أي زائر يكتب رقمه بالطريقة الطبيعية يُمنع من إرسال طلبه. هذا يعطّل قناة العملاء الوحيدة في الموقع.

**كيف تتحقق بنفسك:** افتح الصفحة، اكتب اسمًا ورسالة، وضع الرقم `01229688688` في حقل الهاتف، ثم اضغط إرسال. ستظهر رسالة خطأ.

أو مباشرة في وحدة تحكم المتصفح:

```javascript
/^(\+?20)?1[0-9]{9}$/.test('01229688688')
```

الناتج:

```
false
```

---

### 2. جميع عناوين الأقسام تفقد كسر السطر ولون التمييز

في ملف HTML الأصلي، العنوان مكتوب على سطرين مع تلوين السطر الثاني:

```html
<!-- index.html:1039 -->
<h2 class="sec-title reveal" data-i18n="productsTitle">THREE BRANDS.<br><span style="color:var(--copper)">ONE STANDARD.</span></h2>
```

لكن دالة تبديل اللغة تستبدل المحتوى الداخلي بالكامل:

```javascript
// index.html:2445
document.querySelectorAll('[data-i18n]').forEach(el=>{
  const k=el.getAttribute('data-i18n');
  if(t[k]!==undefined) el.innerHTML=t[k];
});
```

والقيمة المخزّنة في جدول الترجمة تستخدم محرف سطر جديد وليس وسم HTML:

```javascript
productsTitle:'THREE BRANDS.\nONE STANDARD.'
```

محرف السطر الجديد في HTML يُعامل كمسافة عادية ما لم تُضبط خاصية العرض المناسبة. وقد تأكدت أن هذه الخاصية غير مضبوطة على العنصر:

```css
/* index.html:295 — لا توجد خاصية white-space */
.sec-title { font-family: 'Bebas Neue', sans-serif; font-size: clamp(2.5rem, 5vw, 4.5rem); line-height: 1; }
```

المشكلة تحدث عند كل تحميل للصفحة، لأن السطر الأخير في الكود يستدعي الدالة تلقائيًا:

```javascript
// index.html:2470
switchLanguage('en');
```

**نتيجة الاختبار الفعلي بعد تحميل الصفحة:**

```json
[
 { "html": "WHO WE\nARE",              "lines": 1, "hasBr": false, "hasSpan": false },
 { "html": "TRUSTED BY\nEGYPT'S LEADERS", "lines": 1, "hasBr": false, "hasSpan": false },
 { "html": "THREE BRANDS.\nONE STANDARD.", "lines": 1, "hasBr": false, "hasSpan": false },
 { "html": "COMPLETE\nPOWER SOLUTIONS",  "lines": 1, "hasBr": false, "hasSpan": false },
 { "html": "HOW WE\nDELIVER",           "lines": 1, "hasBr": false, "hasSpan": false },
 { "html": "POWER SIZE\nCALCULATOR",    "lines": 1, "hasBr": false, "hasSpan": false }
]
```

كل العناصر أصبحت سطرًا واحدًا، ووسم كسر السطر ووسم التلوين اختفيا تمامًا.

**العناوين المتأثرة — 18 عنوانًا في اللغتين:**

```
amcTitle, calcTitle, certTitle, clientsTitle, compTitle, contactTitle,
faqTitle, fuelInfraTitle, galleryTitle, locTitle, portfolioTitle,
productsTitle, projectsTitle, servicesTitle, soundTitle, tsTitle,
visionTitle, workflowTitle
```

**كيف تتحقق بنفسك:** افتح الصفحة وانزل إلى أي قسم. ستجد العنوان على سطر واحد بلون واحد. ثم في وحدة التحكم:

```javascript
document.querySelector('.sec-title').innerHTML
```

---

### 3. البريد الإلكتروني معطوب ورابطه يؤدي إلى 404

الصفحة تحتوي على بقايا نظام إخفاء البريد الخاص بـ Cloudflare:

```html
<!-- index.html:1752 -->
<a href="/cdn-cgi/l/email-protection#c9a0a7afa689b9bbaca4a0bca4b9a6beacbbe4acaee7aaa6a4"><span class="__cf_email__" data-cfemail="21484f474e615153444c48544c514e5644530c44460f424e4c">[email&#160;protected]</span></a>
```

```html
<!-- index.html:1868 -->
<script data-cfasync="false" src="/cdn-cgi/scripts/5c5dd728/cloudflare-static/email-decode.min.js"></script>
```

هذا المسار تُنشئه شبكة Cloudflare عند مرور الطلب عبرها. لكن حسب دليل المشروع، الاستضافة على GitHub Pages:

> «GitHub Pages ينشر محتوى جذر فرع `main`» — من ملف `PROJECT_GUIDE.md`، القسم 4

وGitHub Pages لا يوفّر هذا المسار، فلا يُحمَّل السكربت أبدًا ولا يُفَك تشفير البريد.

**نتيجة الاختبار الفعلي:**

```
HTTP 404  /cdn-cgi/scripts/5c5dd728/cloudflare-static/email-decode.min.js
عرض البريد: "[email protected]"
رابط البريد: /cdn-cgi/l/email-protection#c9a0a7afa689...
```

أي أن الزائر يرى نصًا حرفيًا لا معنى له، وإذا ضغط عليه يصل إلى صفحة غير موجودة.

**ملاحظة مهمة على الحل:** الإخفاء هنا بلا فائدة أصلًا، لأن البريد مكتوب صراحةً في مكان آخر بالكود:

```javascript
// index.html:2395
window.location.href=`mailto:info@premiumpower-eg.com?subject=...`;
```

**كيف تتحقق على الإنتاج:** (لم أتمكن من فحص الموقع المباشر لأن سياسة الشبكة في بيئة العمل تمنع الاتصال الخارجي، فالفحص أعلاه تم محليًا)

```bash
curl -s -o /dev/null -w "%{http_code}\n" https://www.premiumpower-eg.com/cdn-cgi/scripts/5c5dd728/cloudflare-static/email-decode.min.js
```

إن كانت النتيجة 404 فالمشكلة مؤكدة على الإنتاج. وإن كانت 200 فالموقع خلف Cloudflare فعلًا والمشكلة محلية فقط.

---

## ثانيًا: مشاكل عالية الأهمية

### 4. روابط القائمة العلوية مقصوصة على شاشات اللابتوب الشائعة

القائمة تتحول إلى زر الهامبرغر فقط عند 768 بكسل أو أقل:

```css
/* index.html:832-833 */
.nav-links, .nav-cta { display: none; }
.hamburger { display: block; }
```

لكن عرض الروابط الفعلي يتجاوز المساحة المتاحة قبل ذلك بكثير.

**نتيجة الاختبار الفعلي عبر عدة عروض شاشة:**

| عرض الشاشة | روابط مقصوصة خارج الشاشة |
|---|---|
| 1440 بكسل | لا شيء ✅ |
| 1280 بكسل | Contact ❌ |
| 1100 بكسل | Portfolio, Contact ❌ |
| 900 بكسل | Certifications, Calculator, Portfolio, Contact ❌ |
| 768 بكسل | لا شيء (تحوّلت لقائمة الهامبرغر) ✅ |

المدى المتأثر هو من 769 إلى ~1350 بكسل، وهو مدى شاشات اللابتوب الأكثر شيوعًا: 1024 و1280 و1366.

**الأثر:** رابط «Contact» — وهو أهم رابط تجاري في الصفحة — غير قابل للوصول على شاشة لابتوب بعرض 1280 بكسل.

---

### 5. صور العملاء والمشاريع صور مخزون عامة

قسم «العملاء» يعرض أسماء جهات حقيقية بالاسم فوق صور مخزون من Unsplash:

```html
<!-- index.html:1023 -->
<img src="https://images.unsplash.com/photo-1519494026892-80bbd2d6fd0d?..." alt="Egyptian Ambulance Organization" ...>
<div class="client-name" data-i18n="client1">Egyptian Ambulance Organization</div>
```

والجهات المذكورة بالاسم تشمل:

```
Egyptian Ambulance Organization
Canal For Sugar
SODIC — Ogami North Coast
Concord — New Mansoura
New Urban Communities Authority
Salah Eldin International School
```

وقسم «Recent Work — INSTALLATIONS ACROSS EGYPT» يعرض ثلاثة مشاريع بأوصاف فنية دقيقة فوق صور مخزون أيضًا:

```html
<!-- index.html:1316 -->
<img src="https://images.unsplash.com/photo-1581094288338-2314dddb7ece?..." alt="Medical Center Backup Power - New Cairo" ...>
<p class="proj-detail">2 × 500 KVA Perkins units with full ATS synchronization panel and UPS integration.</p>
```

**دليل قاطع على أنها ليست صورًا حقيقية:** نفس الصورة مستخدمة مرتين لادعاءين مختلفين تمامًا. الصورة التالية:

```
photo-1486406146926-c627a92ad1ab
```

تظهر مرة كصورة العميل «New Urban Communities Authority» في السطر 1030، ومرة كصورة المشروع «Bank HQ Data Center - Maadi» في السطر 1330.

**الأثر:** مخاطرة على المصداقية أمام عميل صناعي يتحقق من المراجع، ومخاطرة قانونية عند نسب صور مخزون إلى جهات حكومية وشركات مسمّاة.

**كيف تتحقق:**

```bash
grep -c 'photo-1486406146926-c627a92ad1ab' index.html
```

الناتج: 3 مرات (مرتان في العملاء بسبب التكرار للحركة، ومرة في المشاريع).

---

### 6. مكتبة أيقونات محمّلة بالكامل بلا أي استخدام

```html
<!-- index.html:1868 -->
<script src="https://unpkg.com/lucide@0.263.1/dist/umd/lucide.js"></script>
```

**نتيجة البحث في الملف كله:**

```
data-lucide          : 0 نتيجة
lucide.createIcons() : 0 نتيجة
أي استخدام آخر       : 0 نتيجة
```

المكتبة تُحمَّل من خادم خارجي عند كل زيارة، ثم لا تُستخدم إطلاقًا. الأيقونات في الموقع كلها رموز نصية ووجوه تعبيرية مكتوبة مباشرة في HTML.

**الأثر:** وقت تحميل ضائع، واعتماد على طرف خارجي، وسطح هجوم في سلسلة التوريد — كل ذلك مقابل صفر فائدة. الحل هو حذف السطر.

---

### 7. الشعار بحجم 1.3 ميجابايت يُستخدم كأيقونة الموقع

```html
<!-- index.html:16-17 -->
<link rel="icon" type="image/png" href="/images/logo.png">
<link rel="apple-touch-icon" href="/images/logo.png">
```

**أبعاد وحجم الملف الفعلي:**

```
images/logo.png : 1385 × 1333 بكسل — 1,344,076 بايت (1.31 ميجابايت)
```

المتصفح يعرض هذه الصورة في تبويب بحجم 16 أو 32 بكسل. أي أن كل زائر يحمّل 1.31 ميجابايت لعرض مربع صغير جدًا.

هذا الملف وحده أكبر من صفحة HTML كاملة (190 كيلوبايت) بسبعة أضعاف.

---

### 8. معرض الصور 8.4 ميجابايت لعرض مربعات 400 بكسل

الصور معلن عنها في HTML بمقاس العرض الفعلي:

```html
<img src="./images/gallery/gen_7.webp" alt="Generator installation 7" loading="lazy" width="400" height="400">
```

**الأبعاد والأحجام الحقيقية للملفات:**

| الملف | الأبعاد الفعلية | الحجم | يُعرض في |
|---|---|---|---|
| `gen_1.webp` | 2528 × 1696 | 233 كيلوبايت | 400 × 400 |
| `gen_2.webp` | 2624 × 1600 | 331 كيلوبايت | 400 × 400 |
| `gen_3.webp` | 4928 × 3280 | 645 كيلوبايت | 400 × 400 |
| `gen_4.webp` | 4928 × 3280 | 849 كيلوبايت | 400 × 400 |
| `gen_5.webp` | 4928 × 3280 | 928 كيلوبايت | 400 × 400 |
| `gen_6.webp` | 5833 × 5357 | 1,594 كيلوبايت | 400 × 400 |
| `gen_7.webp` | 4928 × 3280 | 2,080 كيلوبايت | 400 × 400 |
| `gen_8.webp` | 4928 × 3280 | 1,514 كيلوبايت | 400 × 400 |
| **الإجمالي** | | **8.4 ميجابايت** | |

الملف `gen_6.webp` بمقاس 5833 × 5357 يساوي حوالي 31 ميجابكسل، أي أكبر من مساحة العرض بأكثر من 190 ضعفًا.

خاصية التحميل الكسول موجودة، لكنها تؤجل التحميل فقط ولا تلغيه. أي زائر ينزل إلى قسم المعرض يحمّل 8.4 ميجابايت.

**إجمالي وزن الأصول:** حوالي 13 ميجابايت (8.4 معرض + 1.3 شعار + 4.5 ملفات PDF).

---

## ثالثًا: مشاكل متوسطة

### 9. رابط النسخة العربية يشير إلى صفحة غير موجودة

```html
<!-- index.html:15 -->
<link rel="alternate" hreflang="ar" href="https://premiumpower-eg.com/ar">
```

لا يوجد مجلد `ar` ولا ملف بهذا المسار في المستودع:

```
محتويات الجذر: index.html, CNAME, README.md, PROJECT_GUIDE.md,
               images/, doosan-specs.pdf, perkins-specs.pdf, volvo-specs.pdf
```

العربية مطبّقة عبر جافاسكريبت داخل نفس الصفحة، ولا يوجد لها عنوان مستقل. النتيجة أن محركات البحث تُوجَّه إلى صفحة 404، والنسخة العربية غير قابلة للأرشفة ولا للمشاركة برابط.

مشكلة مرتبطة: اختيار اللغة لا يُحفظ إطلاقًا. لا يوجد استخدام لـ:

```
localStorage
sessionStorage
```

فكل إعادة تحميل ترجع الموقع للإنجليزية.

---

### 10. تعارض بين الرابط المعياري والنطاق الفعلي

الرابط المعياري في الصفحة يشير إلى النطاق بدون بادئة:

```html
<!-- index.html:13 -->
<link rel="canonical" href="https://premiumpower-eg.com">
```

بينما ملف النطاق يحدد النطاق الرسمي بالبادئة:

```
www.premiumpower-eg.com
```

ودليل المشروع يؤكد ذلك:

> «النطاق الأساسي هو `https://www.premiumpower-eg.com/` ويحوّل النطاق بدون `www` إليه» — `PROJECT_GUIDE.md` القسم 4

نفس التعارض في ثمانية مواضع أخرى:

```
السطر 14  hreflang="en"
السطر 15  hreflang="ar"
السطر 25  og:image
السطر 26  og:url
السطر 32  twitter:image
السطر 38  حقول @id و url و image داخل بيانات LocalBusiness
```

**الأثر:** الرابط المعياري يشير إلى عنوان يُحوَّل إلى عنوان آخر، وهو ما يُربك فهرسة محركات البحث.

---

### 11. رؤوس الأمان مكتوبة بطريقة لا تعمل

```html
<!-- index.html:33-35 -->
<meta http-equiv="X-Content-Type-Options" content="nosniff">
<meta http-equiv="X-Frame-Options" content="SAMEORIGIN">
<meta http-equiv="Referrer-Policy" content="strict-origin-when-cross-origin">
```

هذان الرأسان لا يعملان إلا كرؤوس HTTP حقيقية من الخادم، ويتجاهلهما المتصفح تمامًا داخل وسم meta:

```
X-Content-Type-Options
X-Frame-Options
```

**تحذير المتصفح الفعلي أثناء الاختبار:**

```
X-Frame-Options may only be set via an HTTP header sent along with a document.
It may not be set inside <meta>.
```

الرأس الثالث فقط يعمل داخل وسم meta:

```
Referrer-Policy
```

**الأثر:** حماية وهمية. الموقع غير محمي فعليًا من التضمين داخل إطار خارجي. وGitHub Pages لا يسمح بضبط رؤوس مخصصة، فالحل الحقيقي يتطلب طبقة أمامية مثل Cloudflare.

---

### 12. تباين الألوان يفشل في معيار الوصولية

اللون الأساسي للعلامة التجارية:

```css
/* index.html:47 */
--copper: #009CDE;
```

يُستخدم لنصوص صغيرة في أنحاء الموقع. **نتائج قياس التباين الفعلي مقابل الحد المطلوب 4.5:1 للنص العادي:**

| العنصر | حجم الخط | نسبة التباين | المطلوب | النتيجة |
|---|---|---|---|---|
| `.sec-label` على خلفية بيضاء | 11.2 بكسل | 3.08:1 | 4.5:1 | فشل ❌ |
| `.sec-label` على خلفية رمادية | 11.2 بكسل | 2.80:1 | 4.5:1 | فشل ❌ |
| `.value-name` | 20.8 بكسل | 2.87:1 | 4.5:1 | فشل ❌ |
| `.svc-icon` | 20.8 بكسل | 2.35:1 | 4.5:1 | فشل ❌ |
| `.stat-num` (أرقام الإحصائيات) | 56 بكسل | 2.80:1 | 3:1 | فشل ❌ |
| `.hero-tag` على الخلفية المتدرجة | 11.2 بكسل | 3.33:1 إلى 5.39:1 | 4.5:1 | يفشل على الجزء الفاتح من التدرج ⚠️ |

المعيار المخالَف:

```
WCAG 2.1 AA — 1.4.3 Contrast (Minimum)
```

عدد الحالات المميزة التي رصدها الفحص الآلي بعد استبعاد النتائج الخاطئة: حوالي 20 حالة، كلها ناتجة عن نفس اللون.

---

### 13. زر القائمة على الجوال 12 × 15 بكسل

```html
<!-- index.html:880 -->
<button class="hamburger" onclick="app.toggleMenu()" aria-label="Toggle menu" aria-expanded="false" id="hamburger-btn">☰</button>
```

```css
/* index.html:145 — لا يوجد padding ولا أبعاد */
.hamburger { display: none; background: none; border: none; cursor: pointer; color: #fff; }
```

والسبب أن إعادة الضبط العامة تلغي الحشو الافتراضي للزر:

```css
/* index.html:64 */
*, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }
```

فيصبح حجم الزر مساويًا لحجم الرمز النصي فقط.

**القياس الفعلي على شاشة 390 بكسل:**

```
☰  →  12 × 15 بكسل
```

المعيار المخالَف:

```
WCAG 2.2 AA — 2.5.8 Target Size (Minimum) — الحد الأدنى 24 × 24 بكسل
```

والتوصية العملية للمس هي 44 × 44 بكسل. هذا هو **عنصر التنقل الوحيد على الجوال**.

عناصر أخرى دون الحد الأدنى في التذييل:

```
روابط الهاتف والبريد  →  الارتفاع 17 بكسل
```

---

### 14. نافذة ملف المواصفات لا تنقل تركيز لوحة المفاتيح

النافذة معلنة كحوار نمطي:

```html
<!-- index.html:1840 -->
<div id="pdf-modal" role="dialog" aria-modal="true" aria-labelledby="pdf-modal-title" ...>
```

لكن دالة الفتح لا تنقل التركيز إليها:

```javascript
// index.html:2331
function openPdf(brand){
  const c=PDFS[brand]; if(!c) return;
  document.getElementById('pdf-modal-title').textContent=...;
  document.getElementById('pdf-iframe').src=c.file;
  ...
  document.getElementById('pdf-modal').classList.add('open');
  document.body.style.overflow='hidden';
}
```

**نتيجة الاختبار الفعلي بعد فتح النافذة:**

```
العنصر المركَّز عليه: A   داخل النافذة = false
```

أي أن التركيز بقي خلف النافذة. مستخدم لوحة المفاتيح أو قارئ الشاشة يفتح ملف المواصفات ولا يصل إليه.

للمقارنة، قائمة الجوال تنقل التركيز بشكل صحيح:

```javascript
// index.html:2272
if(open){const a=m.querySelector('a');if(a)a.focus();}
```

فالمشكلة في نافذة الـ PDF ونافذة الإشعار فقط، لا في القائمة.

---

### 15. ثلاث مشاكل في حاسبة القدرة

```javascript
// index.html:2351-2356
const v=n=>Math.max(0,Math.min(50,parseInt(document.getElementById(n).value)||0));
const w=(v('c-ac1')*1200)+(v('c-ac3')*2400)+(v('c-pump')*750)+(v('c-pc')*200)+(v('c-light')*100);
const kva=Math.ceil((w/1000/0.8)*1.25);
const fuel=kva>0?Math.ceil(kva*0.21):0;
const cost=fuel*24*12;
```

**أ) القصّ عند 50 صامت ومضلل.** الدالة تحدّ القيمة عند 50 لكنها لا تصحّح ما يراه المستخدم في الحقل.

نتيجة الاختبار الفعلي:

```
إدخال 999 وحدة تكييف  →  النتيجة المعروضة: 102 ك.ف.أ (محسوبة على 50 فقط)
                          الحقل لا يزال يعرض: 999
```

المستخدم يظن أن الرقم صحيح لطلبه بينما هو محسوب على قيمة أخرى تمامًا.

**ب) لوحة النتيجة لا تختفي أبدًا.** المتغير التالي يُضبط مرة واحدة ولا يُعاد ضبطه:

```javascript
// index.html:1876
let calcShown = false;
```

نتيجة الاختبار الفعلي بعد تصفير كل الحقول:

```
القيمة المعروضة: 0 ك.ف.أ
حالة عرض اللوحة: flex   (أي ما تزال ظاهرة)
```

فتبقى لوحة تعرض «0 ك.ف.أ / 0 لتر / 0 جنيه».

**ج) سعر الوقود مثبّت في الكود وفي مكانين منفصلين.** الرقم 12 مكتوب في المعادلة:

```javascript
const cost=fuel*24*12;
```

ومكتوب مرة أخرى في نص الترجمة:

```javascript
calcNote:'*Includes 25% safety margin at 0.8 PF. Motor starting currents may vary. @ 12 EGP/L.'
```

أي تحديث للسعر يجب أن يتم في موضعين وفي اللغتين — أربعة مواضع. وسعر 12 جنيهًا لليتر متجاوَز بالنسبة لسعر السولار الحالي في مصر، مما يجعل «التكلفة اليومية للوقود» المعروضة أقل من الواقع.

كذلك المعادلة تفترض تشغيلًا 24 ساعة يوميًا (الرقم 24 في المعادلة) وهو افتراض غير معلن للمستخدم، وغير واقعي لمولد احتياطي.

---

### 16. النموذج يعرض «تم الإرسال» في كل الحالات

```javascript
// index.html:2394-2398
window.location.href=`mailto:info@premiumpower-eg.com?subject=...&body=...`;
showNotif(t.formSuccess);
document.getElementById('contact-form').reset();
```

الرسالة تُعرض ويُمسح النموذج مباشرة بعد استدعاء بروتوكول البريد، دون أي تحقق من النتيجة.

**السيناريوهات التي تفشل بصمت:**

- جهاز بلا برنامج بريد مُهيّأ — لا يحدث شيء، والمستخدم يرى «تم الإرسال».
- المستخدم يفتح نافذة البريد ثم يغلقها دون إرسال — يرى «تم الإرسال».
- متصفح يحجب بروتوكول `mailto` — يرى «تم الإرسال».

وفي كل هذه الحالات يُمسح النموذج، فيفقد المستخدم ما كتبه ولا توجد وسيلة لاستعادته.

**الأثر التجاري:** لا توجد قناة موثوقة لاستقبال الطلبات. لا يوجد أي استدعاء لخدمة نماذج في الملف كله:

```
fetch(          : 0 نتيجة
XMLHttpRequest  : 0 نتيجة
<form action=   : 0 نتيجة
```

---

## رابعًا: مشاكل منخفضة وملاحظات هيكلية

### 17. ملفات معيارية مفقودة

```
robots.txt   مفقود
sitemap.xml  مفقود
404.html     مفقود
.gitignore   مفقود
```

لافت أن أول ملفين مفقودان في موقع مُحسَّن للبحث بهذه الدرجة (وسوم وصفية كاملة، وبيانات منظمة لنوعين، ووسوم مشاركة اجتماعية).

### 18. تخطٍّ في تسلسل العناوين

```
h2  →  h4 : "Products"
```

قفزة من المستوى الثاني إلى الرابع دون مستوى ثالث بينهما. المعيار المخالَف:

```
WCAG 2.1 A — 1.3.1 Info and Relationships
```

بقية التسلسل سليم: عنوان `h1` واحد فقط من أصل 24 عنوانًا.

### 19. رقم هاتف ثانٍ غير مدرج في البيانات المنظمة

الأرقام الظاهرة في الصفحة:

```
tel:+201229688688      (مرتان)
tel:+201273353355      (مرة واحدة)
wa.me/201229688688     (مرتان)
```

بينما البيانات المنظمة تعلن رقمًا واحدًا فقط في السطر 38:

```
"telephone":"+201229688688"
```

### 20. تعارض بين ساعات العمل المعلنة وادعاء الدعم المستمر

البيانات المنظمة تعلن الأحد إلى الخميس من التاسعة صباحًا حتى الخامسة مساءً:

```
"openingHoursSpecification":[{"dayOfWeek":["Sunday","Monday","Tuesday","Wednesday","Thursday"],"opens":"09:00","closes":"17:00"}]
```

بينما نص الصفحة يَعِد بدعم على مدار الساعة في عدة مواضع:

```
"24/7 support"
"24/7 Emergency Support — Direct line to certified field engineers — day, night, and public holidays"
```

التمييز بين ساعات المكتب وخط الطوارئ مفهوم، لكن الشكل الحالي يبدو متناقضًا لمن يقرأ الاثنين معًا.

### 21. ملاحظات على قابلية الصيانة

```
حجم index.html         : 190 كيلوبايت في ملف واحد
سطور CSS داخل الملف     : 797 سطرًا
سطور JavaScript داخله  : 611 سطرًا
سمات style مضمّنة      : 75 موضعًا
معالجات onclick مضمّنة : 44 معالجًا
```

كل شيء في ملف واحد: البنية والتنسيق والمنطق والترجمة الكاملة للغتين. النتائج المباشرة:

- الملف كاملًا يُعاد تحميله عند كل زيارة، ولا يمكن تخزين التنسيق أو المنطق مؤقتًا بشكل منفصل.
- المعالجات المضمّنة في السمات تمنع تطبيق سياسة أمان محتوى صارمة مستقبلًا.
- تعديل تنسيق واحد قد يتطلب البحث في 75 موضع سمة مضمّنة إضافةً إلى كتلة التنسيق.

---

## خامسًا: ما هو مُنفَّذ بشكل جيد

الإنصاف يقتضي تسجيل نقاط القوة، وهي حقيقية:

- **الترجمة العربية كاملة تمامًا.** الفحص الآلي لكل عنصر مترجَم بعد التبديل للعربية أعطى صفر مفاتيح باقية بالإنجليزية.
- **اتجاه الكتابة من اليمين لليسار مطبّق بشكل صحيح**، مع خط عربي مناسب في القاعدة `body.rtl`.
- **كل الصور الثلاثين تحتوي على نص بديل** وسمة تحميل كسول، و27 منها تحدد الأبعاد صراحةً (مما يمنع اهتزاز التخطيط).
- **حقول النموذج الثلاثة كلها مرتبطة بعناوين صحيحة** عبر السمة `for`، مع `aria-required`.
- **لا توجد معرّفات مكرّرة** في الملف كله.
- **الأسئلة الشائعة متطابقة عددًا** بين البيانات المنظمة والمحتوى المعروض: 10 مقابل 10.
- **رابط تخطي التنقل موجود** ويعمل عند التركيز عليه.
- **مفتاح الهروب يغلق كل النوافذ**، والصور المكررة في الشريط المتحرك مُخفاة عن قارئات الشاشة عبر `aria-hidden="true"` مع نص بديل فارغ — وهذا تفصيل دقيق ومُتقَن.
- **تحسينات أداء موجودة فعلًا**: استخدام `requestAnimationFrame` مع مستمع تمرير سلبي، وتأخير حساب الحاسبة، وربط مسبق للنطاقات الخارجية.

---

## سادسًا: ترتيب مقترح للمعالجة

الترتيب حسب نسبة الأثر إلى الجهد، من الأعلى إلى الأدنى:

| # | المشكلة | الجهد التقريبي | الأثر |
|---|---|---|---|
| 1 | نمط التحقق من الهاتف (البند 1) | سطر واحد | يستعيد قناة العملاء بالكامل |
| 2 | حذف سطر مكتبة الأيقونات غير المستخدمة (البند 6) | سطر واحد | يزيل تبعية خارجية كاملة |
| 3 | إصلاح عناوين الأقسام (البند 2) | سطر واحد في التنسيق أو تعديل الترجمة | يستعيد التصميم في 18 قسمًا |
| 4 | إزالة بقايا إخفاء البريد (البند 3) | سطران | يستعيد بريد التواصل |
| 5 | ضغط الشعار وصور المعرض (البندان 7 و8) | معالجة صور | يوفّر ~12 ميجابايت |
| 6 | نقطة تحوّل القائمة العلوية (البند 4) | سطر واحد في التنسيق | يستعيد التنقل على اللابتوب |
| 7 | حجم زر القائمة على الجوال (البند 13) | سطر واحد | يعالج مخالفة وصولية |
| 8 | توحيد النطاق مع أو بدون البادئة (البند 10) | 9 مواضع | يعالج فهرسة البحث |
| 9 | خدمة استقبال نماذج حقيقية (البند 16) | تكامل خارجي | يجعل الطلبات موثوقة |
| 10 | تعديل لون العلامة للنصوص الصغيرة (البند 12) | متغير تنسيق واحد | يعالج ~20 مخالفة وصولية |

البنود من 1 إلى 4 مجتمعة تقارب أربعة أسطر من التعديل، وتعالج ثلاث مشاكل حرجة وواحدة عالية.

---

## ملحق: كيف أُجري الفحص

الأدوات المستخدمة:

```bash
python3 -m http.server 8123 --directory .
```

ثم متصفح Chromium بلا واجهة عبر مكتبة التحكم، مع فحوص مبرمجة لـ:

- بنية العناوين بعد التحميل (قراءة `innerHTML` و`querySelector('br')`)
- الطلبات الفاشلة ورموز الحالة 4xx
- التجاوب عبر ستة عروض شاشة (1440 و1280 و1100 و900 و768 و390)
- حساب نسب التباين لكل عنصر نصي مرئي وفق معادلة السطوع النسبي المعتمدة
- قياس أبعاد كل عنصر قابل للنقر
- سلوك التركيز بعد فتح النوافذ النمطية
- تنفيذ فعلي لدوال الحاسبة والتحقق من الهاتف وتبديل اللغة

**ما لم أستطع التحقق منه:** حالة الموقع المباشر على النطاق الفعلي، لأن سياسة الشبكة في بيئة العمل تمنع الاتصال الخارجي بالنطاق. البند 3 تحديدًا يحتاج تأكيدًا بأمر واحد على الإنتاج كما هو موضّح في نهاية ذلك البند.
