# دليل مشروع Premium Power Website

آخر مراجعة: 2026-08-12

## 1. الهدف

موقع Premium Power التعريفي العام: خدمات الشركة، المنتجات، بيانات التواصل وملفات المواصفات.

## 2. النسخة الرسمية

- المستودع: `SheedoZ/PremiumPower_Website`
- الفرع الرسمي: `main`
- المسار المحلي المنظم: `D:\Projects\PremiumPower-Website`
- لا تستخدم نسخ `working` أو `source.git` القديمة للتعديل؛ هي محفوظة للأرشيف فقط.

## 3. تركيب الملفات

- `index.html`: الصفحة الرئيسية وكل منطق الواجهة البسيط.
- `images/`: الصور والشعارات.
- ملفات PDF في الجذر: كتالوجات عامة للمنتجات.
- `CNAME`: يربط GitHub Pages بالنطاق `www.premiumpower-eg.com`.
- `README.md`: تعريف مختصر بالمشروع.
- `PROJECT_GUIDE.md`: هذا الدليل التشغيلي.

## 4. التشغيل والإنتاج

الموقع Static ولا يحتاج Node أو قاعدة بيانات. GitHub Pages ينشر محتوى جذر فرع `main`. النطاق الأساسي هو `https://www.premiumpower-eg.com/` ويحوّل النطاق بدون `www` إليه.

## 5. طريقة التعديل الآمنة

1. افتح نسخة `D:\Projects\PremiumPower-Website`.
2. اسحب آخر تغييرات `main`.
3. أنشئ فرعًا مثل `update/home-copy`.
4. عدّل `index.html` أو الصور/الكتالوجات المقصودة فقط.
5. راجع الروابط والصور محليًا، ثم نفّذ الاختبار الصغير أدناه.
6. ارفع التغيير عبر Pull Request، ثم ادمجه في `main` بعد المراجعة.

## 6. اختبار صغير

```powershell
git diff --check
python -m http.server 8080
```

بعد تشغيل الخادم افتح `http://localhost:8080` وتأكد أن الصفحة والشعار والروابط الأساسية تعمل. أوقف الخادم بعد الاختبار.

## 7. النشر

دمج التغيير في `main` يشغّل بناء GitHub Pages تلقائيًا. بعد نجاح البناء راجع الصفحة الرئيسية والنطاقين مع HTTPS. لا يوجد backend يحتاج restart.

## 8. الرجوع لإصدار سابق

استخدم `git revert` للـcommit المسبب للمشكلة، ادمج الـrevert في `main`، وانتظر إعادة بناء Pages ثم راجع الموقع.

## 9. حدود البيانات والأمان

- لا تضف كلمات مرور أو مفاتيح أو ملفات بيئة أو بيانات عملاء.
- لا تضف عروض أسعار أو مستندات تجارية خاصة للمستودع العام.
- بيانات التواصل المنشورة حاليًا بيانات شركة عامة؛ راجع أي تغيير عليها قبل الدمج.
- `CNAME` ملف إنتاج؛ لا تغيّره إلا عند تغيير DNS وGitHub Pages معًا.
